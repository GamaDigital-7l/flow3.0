import React from 'react';
import { Button } from '@/components/ui/button';

const TriggerDailyResetButton = () => {
  return (
    <Button disabled>
      Acionar Daily Reset (Removido)
    </Button>
  );
};

export default TriggerDailyResetButton;